# Email Service Configuration
EMAIL_SERVICE_PORT=3001
EMAIL_SERVICE_HOST=localhost

# Queue Configuration
MAX_RETRIES=3
RETRY_DELAY=5000
PROCESSING_INTERVAL=1000

# Rate Limiting
MAX_REQUESTS_PER_MINUTE=100
RATE_LIMIT_WINDOW=60000

# DNS Configuration
DNS_TIMEOUT=10000
DNS_RETRIES=3

# SMTP Configuration
SMTP_TIMEOUT=30000
SMTP_MAX_CONNECTIONS=10

# Express Server URL (for integration)
EXPRESS_URL=http://localhost:3000

# MongoDB URL (if needed for persistence)
MONGODB_URL=mongodb://localhost:27017/orbitmail 